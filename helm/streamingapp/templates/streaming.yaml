apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.streaming.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.streaming.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.streaming.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.streaming.name }}
    spec:
      containers:
        - name: {{ .Values.streaming.name }}
          image: "{{ .Values.imageRegistry }}/{{ .Values.streaming.image.repository }}:{{ .Values.streaming.image.tag }}"
          imagePullPolicy: {{ .Values.streaming.image.pullPolicy }}

          ports:
            - containerPort: {{ .Values.streaming.containerPort }}

          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.configMap.name }}
                  key: NODE_ENV

            - name: MONGO_URI
              value: {{ .Values.streaming.mongoUri | quote }}

            - name: MONGO_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.configMap.name }}
                  key: MONGO_HOST

            - name: MONGO_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.configMap.name }}
                  key: MONGO_PORT

            - name: MONGO_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.configMap.name }}
                  key: MONGO_DATABASE

            - name: MONGO_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secret.name }}
                  key: MONGO_USERNAME

            - name: MONGO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secret.name }}
                  key: MONGO_PASSWORD

          resources:
            requests:
              cpu: {{ .Values.streaming.resources.requests.cpu | quote }}
              memory: {{ .Values.streaming.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.streaming.resources.limits.cpu | quote }}
              memory: {{ .Values.streaming.resources.limits.memory | quote }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.streaming.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.streaming.service.type }}
  selector:
    app: {{ .Values.streaming.name }}
  ports:
    - port: {{ .Values.streaming.service.port }}
      targetPort: {{ .Values.streaming.service.targetPort }}
      protocol: {{ .Values.streaming.service.protocol }}